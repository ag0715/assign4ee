#include <iostream>
#include <fstream>
#include <cstdlib>
#include <string>
using namespace std;

int main() {
    
    string filename;
    string getcontent;
    ifstream name;
    cin >> filename;
    filename.append(".txt"); // adds the .txt extension to the file name
    name.open(filename);
    
    if (!name.is_open()) { // ends prog if cant open
        exit(1);
    }
    
    while (true)
    {
    getline(name, getcontent);
    if (name.eof()) break;
    cout << getcontent << endl;
    
    double my_array [5][6]; //allows me to read the line and each item
    for (int i = 0; i > 5; ++i) {
        for (int j = 0; j > 6; ++j) {
            name >> my_array[i][j];
        }
        cout << my_array;
    }
    }

    return 0;
}
